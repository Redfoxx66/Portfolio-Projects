<!DOCTYPE html>
<html lang="en-US">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <!-- Insert title of page below -->

    <!--Bootstrap link-->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6"
      crossorigin="anonymous"
    />

    <!-- END SHARED -->
    <title><%- user.userName%></title>
    <link rel="stylesheet" href="../../../../../stylesheets/individualUserStyle.css" />
    <link rel="stylesheet" href="../../../../../stylesheets/mainstlye.css" />
  </head>

  <body>
    <div class="mainheading">
      <header>
        <h1>Box Office Database</h1>
      </header>

      <nav class="mainlinks">
        <div class="maincontent">
          <ul>
            <li><a title="Home page" href="../../../index.html">Home</a></li>
            <li>
              <a title="Movie home page" href="/movies/">Movies</a>
            </li>
            <li>
              <a title="User's Data Page" href="../../UserData.html">User data</a>
            </li>
            <li>
              <a title="Actors home page" href="../../Actors.html">Actors</a>
            </li>
          </ul>
        </div>
      </nav>
    </div>
    <div class="maincontent">
      <%if(user.userName != "") {%>
      <h2>Edit <%-user.userName%></h2>
      <% } else { %>
      <h2>Create a new user</h2>
      <% } %>
      <form method="POST" action="/users/update/<%-user._id%>">
        <input type="hidden" name="id" value="<%-user._id%>" />
        <div class="formSection">
          <label for="userName" class="form-label formDesc">Username:</label>
          <input type="text" class="formInput" name="userName" value="<%-user.userName%>"/>
        </div>

        <div class="formSection">
          <label for="passWord" class="formDesc">Password:</label>
          <input type="text" class="formInput" name="passWord" value="<%-user.passWord%>"/>
        </div>

        <div class="formSection">
          <label for="date" class="formDesc">Date of Account Creation:</label>
          <input type="Date" class="formInput" name="date" value="<%-user.dateCreated%>"/>
        </div>

        <div class="formSection">
          <label for="favMovies" class="formDesc">Favorite Movies:</label>
          <textarea name="favMovies" id="favMovies" class="formInput" columns="30">
          <%- movies.join("\n") -%>
          </textarea>
          <div id="movieHelp" class="form-text">Enter one movie per line.</div>
        </div>

        <div class="formSection">
          <label for="favActors" class="formDesc">Favorite Actors:</label>
          <textarea name="favActors" id="favActors" class="formInput" columns="30">
          <%- actors.join("\n") -%>
          </textarea>
          <div id="actorHelp" class="form-text">Enter one actor per line.</div>
        </div>

        <%if(user.userName != "") {%>
        <button type="submit" class="btn btn-dark btn-lg center formButton" href="./users/create">
          UPDATE USER
        </button>
        <% } else { %>
        <button type="submit" class="btn btn-dark btn-lg center formButton" href="./users/create">
          CREATE USER
        </button>
        <%}%>
      </form>
    </div>

    <footer style="margin-top: 5%">
      <div>
        <span style="font-size: 1.5em">Page by JMM</span>
        <p>
          © <time datetime="2021-4-26">April 2021</time> <br />
          BoxOffice.com
        </p>
      </div>
    </footer>
  </body>
</html>
